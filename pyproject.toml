[project]
name = "news-claw"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "feedparser>=6.0.11",
  "httpx>=0.27",
  "python-dateutil>=2.9",
  "python-dotenv>=1.0",
  "google-generativeai>=0.8",
  "pydantic>=2.7",
]

[tool.setuptools.packages.find]
include = ["fetcher*"]